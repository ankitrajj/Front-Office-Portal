package in.ankit.constants;

public class AppConstants {
	
	/* We can replace any string literal from code with theirs equivalent constants except URLs used in mapping.
	 * 
	 * We can use Interface, Enum, and Class for string literal replacement.
	 */
	
	 public static final String STR_SUCCESS = "success";
	 public static final String STR_LOCKED = "Locked";
}
